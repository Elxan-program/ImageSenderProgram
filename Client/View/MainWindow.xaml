<Window x:Class="Client.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Client.View"
        mc:Ignorable="d"
        Title="Server E1000" Height="425" Width="450">
    <Grid>
        <StackPanel Background="DeepSkyBlue">

            <Image Width="300" Height="200" Name="image1">

            </Image>
            <Button BorderThickness="0" Margin="0,25,0,30" Background="LightSkyBlue" Command="{Binding SelectBtnCommand}" Name="SelectImageButton" Width="300" Content="Select" Height="60"></Button>

            <Button BorderThickness="0" Background="LightSkyBlue" Name="SendImageButton" Width="300" Content="Send" Height="60" Command="{Binding SendBtnCommand}"></Button>
        </StackPanel>
    </Grid>
</Window>
